'use sctrict'

require('dotenv').config({ path: __dirname + '/.env' })
require('./worker')

// fully BASED on
// https://github.com/matszrmn/NodeJS_Copy_Collection/tree/master/src

// 1. first of all create/change your .env
// 2. run with "yarn dev" or "yarn start" (it's the same)
